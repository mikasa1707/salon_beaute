import { Permission } from './permission';

describe('Permission', () => {
  it('should create an instance', () => {
    const directive = new Permission();
    expect(directive).toBeTruthy();
  });
});
